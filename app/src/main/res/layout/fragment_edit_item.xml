<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@android:color/white" >

    <EditText
        android:id="@+id/etUpdateItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/etUpdatePriorityPicker"
        android:layout_alignParentEnd="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:inputType="textMultiLine" />

    <EditText
        android:id="@+id/etUpdatePriorityPicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnUpdateItem"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/etUpdateTimePicker"
        android:layout_toLeftOf="@+id/etUpdateDatePicker"
        android:focusable="false"
        android:hint="Priority" />

    <EditText
        android:id="@+id/etUpdateDatePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnUpdateItem"
        android:layout_alignTop="@+id/etUpdateTimePicker"
        android:layout_toLeftOf="@+id/etUpdateTimePicker"
        android:gravity="center"
        android:focusable="false"
        android:hint="Pick a date" />

    <EditText
        android:id="@+id/etUpdateTimePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnUpdateItem"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:gravity="center"
        android:focusable="false"
        android:hint="Pick a time" />

    <Button
        android:id="@+id/btnUpdateItem"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="260dp"
        android:onClick="onUpdateItem"
        android:text="UPDATE" />

</RelativeLayout>
